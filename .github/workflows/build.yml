name: Build and Push Docker Image to GHCR

on:
  push:
    branches:
      - main

jobs:
  build-and-publish:
    uses: hollowbytes/hb-shared-workflows/.github/workflows/ci-docker-build.yml
    with:
      file: ./src/HbDotnetBff/Dockerfile
